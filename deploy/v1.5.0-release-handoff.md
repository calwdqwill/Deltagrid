# v1.5.0 release handoff

## Текущее состояние

- Рабочая ветка: `codex/v1.4.1-funding-release-tooling`.
- Текущий commit ветки: `0a43813 chore: prepare v1.5.0 funding qa release`.
- Base branch для review: `main`.
- Текущий `origin/main`: `2b6c830 docs: record v1.4.0 production release`.
- Production runtime остаётся на release commit `3936c83` с `VERSION=1.4.0`.
- Tag `v1.4.0` существует; tags `v1.4.1` и `v1.5.0` не созданы.
- Открытый PR для ветки не найден.
- `gh` CLI и `GITHUB_TOKEN`/`GH_TOKEN` в локальной сессии недоступны, поэтому PR нужно открыть вручную.

## PR

URL для открытия PR:

```text
https://github.com/calwdqwill/Deltagrid/compare/main...codex/v1.4.1-funding-release-tooling?expand=1
```

Рекомендуемый заголовок:

```text
chore: prepare v1.5.0 funding qa release
```

Рекомендуемый body:

```markdown
## Что изменилось

- Подготовлен `v1.5.0` как Funding/Data QA minor release поверх production `v1.4.0`.
- Добавлен read-only `Funding Data Quality Runway` во frontend Funding.
- Funding smoke contract получил `data_quality_runway` summary.
- Funding release report, validator и CI evidence bundle учитывают runway gates.
- Версии подняты до `1.5.0` в `VERSION`, `frontend/package.json`, `frontend/package-lock.json` и README.

## Проверки

- `./scripts/release-preflight.sh 1.5.0`
- `RELEASE_TARGET=1.5.0 RELEASE_CHECK_DOCS=1 ./scripts/release-preflight.sh`
- `npm run build` во `frontend`
- `npm audit --audit-level=high` во `frontend`
- `bash -n scripts/funding-qa-smoke.sh`
- `bash -n scripts/funding-release-report.sh`
- `bash -n scripts/funding-release-report-validate.sh`
- `bash -n scripts/funding-release-ci-report.sh`
- Funding compact report + validator на temp artifact
- CI-like Funding evidence bundle на temp artifact

## Safety boundary

- Backend API не менялся.
- БД и миграции не менялись.
- Provider calls не добавлялись.
- Trading, execution, route ranking, route selection, route cost bps и diagnostic carry bps не включались.

## Release gate после PR

- Дождаться GitHub CI на PR.
- Получить preview evidence перед merge в `main`.
- Перед production deploy сделать PostgreSQL backup.
- После deploy пройти production smoke и Browser QA.
- Создать annotated tag `v1.5.0` только после подтверждённого production runtime.
```

## Локальные checks

Уже пройдены на `0a43813`:

```bash
./scripts/release-preflight.sh 1.5.0
RELEASE_TARGET=1.5.0 RELEASE_CHECK_DOCS=1 ./scripts/release-preflight.sh
npm run build
npm audit --audit-level=high
bash -n scripts/funding-qa-smoke.sh
bash -n scripts/funding-release-report.sh
bash -n scripts/funding-release-report-validate.sh
bash -n scripts/funding-release-ci-report.sh
```

`npm audit --audit-level=high` не нашёл high/critical blocker. Известный moderate `postcss` внутри bundled Next.js остаётся неблокирующим tech debt до stable Next.js patch.

## Preview evidence

После PR/review нужно получить один из двух вариантов preview evidence:

1. Merge/cherry-pick в `preview`, дождаться `CI` и `Deploy Preview`, затем запустить preview Funding release report.
2. Если GitHub runner снова упирается в SSH reachability, использовать уже описанный public HTTP fallback только после фактической доставки commit на preview.

Минимальные команды для preview evidence:

```bash
BASE_URL=http://127.0.0.1:8011 FRONTEND_URL=http://127.0.0.1:3012 sh scripts/funding-qa-smoke.sh
BASE_URL=http://127.0.0.1:8011 FRONTEND_URL=http://127.0.0.1:3012 COMPARE_BASE_URL=http://127.0.0.1:8000 sh scripts/funding-release-ci-report.sh
```

Если `COMPARE_BASE_URL` недоступен или funding rows пустые, blocked report остаётся валидным evidence artifact, но не является release-ready approval.

## Production release gate

Не выполнять production deploy и tag из feature branch.

Финальная последовательность:

1. PR review и зелёный CI.
2. Preview evidence приложен к PR или release notes.
3. Merge в `main`.
4. PostgreSQL backup перед deploy.
5. Production deploy.
6. `scripts/release-smoke.sh` на production.
7. Browser QA production для `/version`, `/funding`, `/funding?view=qa`, `/data-health` и ключевых terminal screens.
8. Проверить `/version` и `VERSION=1.5.0` на runtime.
9. Создать annotated tag `v1.5.0`.

## Запреты

До отдельного продуктового решения не включать:

- trading;
- execution;
- route ranking;
- route selection;
- route cost bps;
- diagnostic carry bps;
- новые provider calls;
- изменения backend API или БД.
